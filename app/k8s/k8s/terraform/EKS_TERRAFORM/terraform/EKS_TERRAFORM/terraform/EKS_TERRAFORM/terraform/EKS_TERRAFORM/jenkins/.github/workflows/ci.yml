name: CI (placeholder)
on:
  workflow_dispatch:  # manual run only; no auto triggers
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Safe placeholder CI. Nothing deploys."
